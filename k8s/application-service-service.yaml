apiVersion: v1
kind: Service
metadata:
  name: application-service
spec:
  type: NodePort
  selector:
    app: application-service
  ports:
    - port: 8083
      targetPort: 8083
      nodePort: 30083
